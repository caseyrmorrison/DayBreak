# Sync vault storage. Defaults to a local SQLite file (file:data/daybreak-sync.db).
# For cloud deploys (e.g. Vercel), create a free Turso database and set:
# SYNC_DB_URL=libsql://<your-db>.turso.io
# SYNC_DB_AUTH_TOKEN=<token>
